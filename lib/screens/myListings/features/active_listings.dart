import 'package:flutter/material.dart';

void main() => runApp(const MaterialApp(
      home: ActiveListings(),
    ));

class ActiveListings extends StatelessWidget {
  const ActiveListings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(

    );
  }
}
