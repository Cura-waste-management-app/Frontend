import 'package:flutter/material.dart';

void main() => runApp(const MaterialApp(
      home: SharedListings(),
    ));

class SharedListings extends StatelessWidget {
  const SharedListings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(

    );
  }
}
